import styles from "./Section.module.css";

const SectionWrapper = function () {
  <div className={`${styles.section} ${styles.banner}`}>
    <h1>hello</h1>
  </div>;
};

export default SectionWrapper;
